# 项目规则

## 弹窗设计

- 后台弹窗默认尽量压缩在一屏内完成，不依赖纵向滚动。
- 表单弹窗优先使用左右分栏、网格、短文案和紧凑间距。
- 同一业务含义只保留一个入口，避免“其他问题”和“异常记录”这类重复按钮。
- 弹窗说明文案只保留必要提醒，不写长段解释。
- 若内容确实超过一屏，应优先拆成步骤或单独详情页，而不是简单加滚动。

## 验证命令

- 修改前后端代码后运行 `git diff --check` 和 `npm run build:tencent`。
- 涉及租户/店家数据隔离时运行 `npm run test:tenant-isolation`。
