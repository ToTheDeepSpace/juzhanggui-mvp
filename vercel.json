{
  "buildCommand": "npm run build",
  "outputDirectory": "dist/client",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/health2", "destination": "/api/health" },
    { "source": "/api/(.*)", "destination": "/api/index" }
  ]
}
