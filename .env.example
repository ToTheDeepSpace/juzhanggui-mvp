# 前端环境变量
# 复制此文件为 .env 后填写实际值

# 签到页面的基础 URL（用于生成二维码）
# 开发环境：留空，使用 window.location.origin
# 生产环境：填入实际域名，如 https://your-domain.com
VITE_CHECKIN_BASE_URL=

# API 代理路径（开发用，通过 vite proxy 转发到后端）
# 生产环境可忽略，通过后端服务统一暴露
VITE_API_PREFIX=/api
